---
interface Props {
    name: string
}
const {name} = Astro.props
---

<div class="flex flex-col w-75">
    <h3 class="ml-1 mb-1 uppercase text-xs font-semibold text-white/90">{name}</h3>
    <div class="flex flex-col">
        <slot />
    </div>
</div>
