---
interface Props {
    value: string
}
const {value} = Astro.props
---

<input
    class="inline-flex font-mono text-white text-[0.8rem]"
    style={{
        width: `${value.length}ch`,
    }}
    readonly
    value={value}
/>
